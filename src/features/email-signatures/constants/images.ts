// Constantes pour les images par défaut utilisées dans les signatures email
import placeholderProfileImage from '../../../assets/placeholderProfile.png';

// Image par défaut pour la photo de profil
export const DEFAULT_PROFILE_IMAGE = placeholderProfileImage;

// Image par défaut pour le logo
export const DEFAULT_LOGO_IMAGE = '/images/logo_newbi/SVG/Logo_Texte_Purple.svg';

// Taille par défaut pour la photo de profil (en pixels)
export const DEFAULT_PROFILE_PHOTO_SIZE = 80;
